# Copy this file to `.env` and fill in your key. `.env` is gitignored — never commit it.
DEEPSEEK_API_KEY=

# Optional: enables real web search for fetch_company_brief (company legitimacy).
# Leave blank to disable — the tool then degrades to JD-only judgement. Get one at tavily.com.
TAVILY_API_KEY=
